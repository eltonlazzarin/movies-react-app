import styled from 'styled-components';

export const Container = styled.div`
  span {
    display: block;
    text-align: center;
    padding-top: 8.31rem;

    h4 {
      color: var(--white);
      font-size: 1.3rem;
      margin-top: 1rem;
    }

    a {
      font-size: 1.2rem;
      color: var(--gray);
      text-decoration: none;
    }

    img {
      width: 8rem;
      height: 4.3rem;
    }
  }
`;
