import styled from 'styled-components';

export const Container = styled.div`
  max-width: 66.25rem;
  margin: 4.5rem auto;

  font-size: 0.94rem;
  font-weight: bold;

  @media (max-width: 46rem) {
    width: 90vw;
  }
`;
